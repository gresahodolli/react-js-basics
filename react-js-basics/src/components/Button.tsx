import React from "react";

const Button = () => {
  return (
    <div>
      <button type="button" className="btn btn-primary">
        BYE!
      </button>
    </div>
  );
};

export default Button;
